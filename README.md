🖱️ Autoclicker (Win32 API – C)

A simple autoclicker written in C using the Win32 API.

🚀 How It Works

Hold the Tilde key (`) to activate the autoclicker.

Press Esc to exit the program.

🛠️ Requirements

Windows OS

C compiler (e.g., MinGW or MSVC)

▶️ Build & Run

Compile the source file using your preferred C compiler, then run the generated executable.

or simply download the executable file and run 

autoclicker.exe
